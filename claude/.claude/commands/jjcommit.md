---
description: Run jj commit
argument-hint: [context]
---

Create a commit using `jj commit`. If there is another message after this line, use it as the context for creating the commit; otherwise come up with a reasonable, detailed commit message.

$ARGUMENTS
